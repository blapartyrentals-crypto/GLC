{
  "name": "@glc/engine-partner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@glc/contracts": "workspace:*"
  },
  "devDependencies": {
    "@glc/tsconfig": "workspace:*",
    "typescript": "^5.6.0"
  }
}
