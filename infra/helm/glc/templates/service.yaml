apiVersion: v1
kind: Service
metadata:
  name: {{ include "glc.name" . }}
  labels:
    app: {{ include "glc.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "glc.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http